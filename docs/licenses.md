# D5.12 — Licenses

Project decision: **MIT** (see `LICENSE`, copyright held by the ADAPT
team: Adith K P, Akash Rajeev K V, Abindas P, Alan B). The MIT license
covers **ADAPT-authored** code, prose, and generated data only. Every
third-party component and research asset retains its own license, as
inventoried below and in `provenance/`. MIT status of the repository as
a whole must not be read as a statement about referenced/published
third-party work.

## Source code (ours)

All `.py`, `.html`, `.css`, `.js`, configuration, test, and script
files were written for this project (Phases 1–5). See
`provenance/code.csv`.

## Data (ours, generated)

All task data is procedurally generated in-repo (seeded RNG, no
external corpus) — unencumbered. Learner-session data is consent-scoped
and not published. See `provenance/data.csv`.

## Models / checkpoints

- Controlled strategies and learned trio checkpoints: our training /
  hand-specified dynamics — our license applies.
- BDH / BDH-CQ **weights are NOT distributed here**; they are referenced
  by paper/repo citation only (their analysis remains with their
  authors). See `provenance/models.csv`.

## Graphics

Figures fig1–fig8 are generated by our own `make_figures.py` from our
own results — ours. No paper figures are reused; a paper concept that
is shown is redrawn with attribution. The BDH stepper/synapse cards in
the UI are our educational renderings of a published structure,
explicitly labelled ILLUSTRATIVE. See `provenance/assets.csv`.

## Fonts

No webfonts — system font stack only (`provenance/fonts.csv`). Zero font
licensing exposure.

## Third-party libraries

| Package | License | Use |
|---|---|---|
| numpy ≥1.24 | BSD-3-Clause | engine + analysis (verified 2.4.6) |
| pyyaml ≥6.0 | MIT | configs (verified 6.0.3) |
| matplotlib ≥3.7 | PSF-based | figures (verified 3.11.1) |
| pytest ≥7.0 | MIT | tests (verified 9.1.1) |
| torch 2.14.0+cpu | BSD-3-Clause | **repro env only** (R1/R2); not on learner path |

No JS dependencies; no bundler; nothing injected over the network.

## Paper figures (audit)

Policy (D5.9 / §59): if a paper figure cannot be legally reused, redraw
it ourselves based on the concept, with attribution — which is what we
do. We ship none of the papers' figures, icons, or text.

## References, not redistribution

Papers and official repositories are cited precisely where claims use
them (`docs/sources.md`); citing is not redistributing. Published
research (e.g. BDH / BDH-CQ papers) remains copyright of its authors and
publishers; any referenced external model/weights remain under their own
specified terms (`provenance/models.csv`). Third-party libraries are
listed above with their licenses (`provenance/third_party.csv`). The MIT
license applies only to ADAPT's original work.